# 修改意见

> 更新日期: 2026-04-15 | 基于 V2-V4 全部实验结果 + 架构分析

---

## 阶段总结

| 阶段 | 内容 | 结果 |
|------|------|------|
| V2 | 首次 DDPM latent 验证 | 负结果 |
| V3 | DDPM 超参/curriculum/residual 穷举（6 候选）| 全部 fail |
| V4 | Bug fix (double-tanh) + 重验证 | hard delta = -0.138, Suspend |

**V4 结论**: DDPM latent diffusion 在 bug-free 代码上仍不过 gate。
问题不是超参或 bug，而是架构层面的结构性不匹配。

---

## 失败根因分析

1. **迭代去噪引入累积误差** — 10 步去噪在 8 维空间无优势
2. **噪声预测是间接目标** — predict eps 再反推 x0，不如直接预测
3. **latent 空间确定性** — teacher 对每个观测只有一个正确 latent，
   不存在需要 diffusion 建模的多模态分布

---

## V5 方向（PLANS_v5）

引入两种新蒸馏形式，验证 diffusion 家族在低维场景的适用性边界：

### Consistency Distillation
- 单步映射 x_T → x_0，消除迭代误差
- 新文件: `consistency_latent_student.py`

### Flow Matching (Rectified Flow)
- ODE 速度场，确定性单步推理
- 理论上最适合蒸馏任务（确定性映射）
- 新文件: `flow_matching_latent_student.py`

### 预期产出
- 与 DDPM / PAdapt 的统一对比表
- Diffusion 家族在灵巧手低维蒸馏中的完整结论
- 论文级负结果分析（或正面结果）

---

## 已完成的代码修复（V4）

1. `diffusion_latent_student.py:698` — 移除 double-tanh（训练路径）
2. `diffusion_latent_student.py:277` — 移除 double-tanh（推理路径）
3. `diffusion_latent_student.py:666` — 添加 teacher_mu clamp（一致性）
